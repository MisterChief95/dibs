---
agent: agent
description: List dibs tasks and their current status
---
Run `python .github/skills/dibs/scripts/dibs.py list --workspace . --json` in the terminal, then present the tasks as a compact table (ID, title, status, priority, owner if claimed). Do not print raw JSON.
