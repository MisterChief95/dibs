---
mode: agent
description: List dibs tasks that are ready to claim (dependencies satisfied)
---
Run `python .github/skills/dibs/scripts/dibs.py next --workspace . --json` in the terminal, then list the ready tasks (ID, title, priority), or say none are ready. Do not print raw JSON.
